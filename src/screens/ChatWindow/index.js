import React from 'react'
import MessageHeader from '../../components/MessageHeader';
export default function index() {
  // triggering events to connect to socket chat room and to disconnect on leaving the room.
  return (
    // Calling components
    // Message Header
    <MessageHeader/>
    // Message Composer
  )
}
