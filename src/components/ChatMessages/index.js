import React from 'react'

// component will load messages from api's
export default function index() {
  // using caching to prevent frequent data polling when the recepient is offline
  return (
    <div>Chat Messages</div>
  )
}
